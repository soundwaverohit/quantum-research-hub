"""Focused, narrow research agents driven by the orchestrator.

Each agent does one job, calls the shared tool layer, respects the budget, and
logs every action to ``agent_events``. No single all-purpose agent.
"""
